@page
@model DevExpress.UI.Pages.MainPage.PaginaPrincipalModel
@{
}

<head>
	<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.2/font/bootstrap-icons.min.css">
	<link rel="stylesheet" type="text/css" href="~/css/own/general-style.css" />
	<link rel="stylesheet" type="text/css" href="~/css/own/paginaprincipal-style.css" />
	<link rel="stylesheet" type="text/css" href="~/css/own/folio-style.css">
</head>

<h1>PAGINA PRINCIPAL</h1>

<div class="centrar">
	
	<div class="boton-container centrar">
		<div class="texto-container">
			<div class="texto-style-paginaprincipal">
				<h4>Usuarios</h4>
				<p>En usuarios podras observar la lista de usuarios registrados, tambien podras registrar, editar y eliminar usuarios</p>
			</div>
		</div>	
		@(Html.DevExtreme().Button()
			.Width("90%")
			.Text("Ir")
			.Type(ButtonType.Default)
			.Icon("bi bi-person-badge-fill")
			.OnClick("function () {window.location.href = '/Mantenimiento/Usuarios';}")
			)
		
	</div>
	<div class="boton-container centrar">
		<div class="texto-container">
			<div class="texto-style-paginaprincipal">
				<h4>Pedidos</h4>
				<p>En pedidos podras observar una lista de todos los pedidos dados de alta, tambien podras registrar, editar y eliminar pedidos</p>
			</div>
		</div>
		@(Html.DevExtreme().Button()
			.Width("90%")
			.Text("Ir")
			.Type(ButtonType.Default)
			.Icon("bi bi-box-seam-fill")
			.OnClick("function () {window.location.href = '/GeneradorPedidos/Pedidos';}")
		)
	</div>
	<div class="boton-container centrar">
		<div class="texto-container">
			<div class="texto-style-paginaprincipal">
				<h4>Folios</h4>
				<p>En folios podras observar la lista de folios registrados, tambien podras registrar folios dando un rango de folios</p>
			</div>
		</div>

		@(Html.DevExtreme().Button()
			.Width("90%")
			.Text("Ir")
			.Type(ButtonType.Default)
			.Icon("bi bi-card-list")
			.OnClick("function () {window.location.href = '/GeneradorFolios/Folios';}")
			)	
	</div>
</div>